const state = {
  articles: [],
  current_article: {},
};

const getters = {
  articles: state => {
    // TODO: read files from articles folder
  }
};

const actions = {
  
};

const mutations = {

};

export default {
  namespaced: true,
  state, 
  getters,
  actions,
  mutations
}